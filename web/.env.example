ANTHROPIC_API_KEY=sk-ant-...

# Set to 1 to run peer review on Anthropic Claude Managed Agents
# (each peer becomes its own agent, each review runs as its own session).
# Leave unset to use the regular messages.create + tool-use path.
MANAGED_AGENTS=
